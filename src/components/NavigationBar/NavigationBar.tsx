import React from "react";
import { View } from "react-native";

interface Props {
  showBackButton?: boolean;
}

const NavigationBar = ({ showBackButton }: Props) => {
  return <View></View>;
};

export default NavigationBar;
